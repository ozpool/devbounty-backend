name: keep-render-alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend liveness endpoint
        run: curl --fail --silent --show-error https://devbounty-backend.onrender.com/health
